interface:
  display_name: "CCAT"
  short_description: "Repo-specific patterns and workflows for CCAT"
  default_prompt: "Use the CCAT repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true